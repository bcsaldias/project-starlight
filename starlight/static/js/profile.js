// Generated by CoffeeScript 1.10.0
$(function() {
  var context, url, username;
  context = {
    'readOnly': true,
    'angleOffset': -125,
    'angleArc': 250,
    'thickness': 0.2,
    'fgColor': "#8008BC",
    'bgColor': "#999999"
  };
  $('#points').knob(context);
  username = $('#username').attr('data-value');
  url = '/user/' + username + '/activities';
  return $.getJSON(url, function(result) {
    var activities, activity, i, len, results;
    activities = result['activities'];
    results = [];
    for (i = 0, len = activities.length; i < len; i++) {
      activity = activities[i];
      results.push(console.log(activity));
    }
    return results;
  });
});
